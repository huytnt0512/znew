const LEFT = "lefts";
const GET_ALL_LEFT = `${LEFT}/getAllLeft`
const SAVE_ALL_LEFT = `${LEFT}/saveAllLeft`

const CREATE_LEFT = `${LEFT}/createLeft`
const SAVE_NEW_LEFT = `${LEFT}/saveNewLeft`

const UPDATE_LEFT = `${LEFT}/updateLeft`
const SAVE_UPDATE_LEFT = `${LEFT}/saveUpdateLeft`

const DELETE_LEFT = `${LEFT}/deleteLeft`

const GET_DETAIL_LEFT = `${LEFT}/getDetailLeft`
const SAVE_DETAIL_LEFT = `${LEFT}/saveDetailLeft`

const GET_MULTIPLE_LEFT = `${LEFT}/getMultipleLeft`
const SAVE_MULTIPLE_LEFT = `${LEFT}/saveMultipleLeft`

const GET_FILTER_LEFT = `${LEFT}/getFilterLeft`
const SAVE_FILTER_LEFT = `${LEFT}/saveFilterLeft`

export {
    LEFT,
    GET_ALL_LEFT,
    SAVE_ALL_LEFT,
    CREATE_LEFT,
    SAVE_NEW_LEFT,
    UPDATE_LEFT,
    SAVE_UPDATE_LEFT,
    DELETE_LEFT,
    GET_DETAIL_LEFT,
    SAVE_DETAIL_LEFT,

    GET_MULTIPLE_LEFT,
    SAVE_MULTIPLE_LEFT,

    GET_FILTER_LEFT,
    SAVE_FILTER_LEFT
}